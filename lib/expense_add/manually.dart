import 'package:flutter/material.dart';

class ManuallyAddPage extends StatelessWidget {
  const ManuallyAddPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("I am a widget that can be used in your app!");
  }
}